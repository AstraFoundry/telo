# Deployment

Telo is a packaged desktop app and does not use Docker or Kubernetes. Release configuration lives in `package.json`; automation lives in `.github/workflows/release.yml`. See [build and release](../docs/operations/deployment.md).
